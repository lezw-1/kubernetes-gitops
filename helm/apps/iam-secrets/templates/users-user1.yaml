# User Secret: user1.
apiVersion: v1
kind: Secret
metadata:
  # Secret resource name.
  name: users-user1

  # Namespace set by the Argo CD Application's destination.
  namespace: {{ .Release.Namespace }}

# Opaque: arbitrary key/value data, not one of Kubernetes' typed secrets (e.g. tls, basic-auth).
type: Opaque

stringData:
  # User definition, read from values.secrets.users.user1.
  email: {{ .Values.secrets.users.user1.email }}
  firstName: {{ .Values.secrets.users.user1.firstName }}
  lastName: {{ .Values.secrets.users.user1.lastName }}
  enabled: {{ .Values.secrets.users.user1.enabled }}
  emailVerified: {{ .Values.secrets.users.user1.emailVerified }}

  # Login credentials — must be supplied by a real environment override;
  # chart/env defaults are intentionally empty (see values.yaml comments).
  username: {{ .Values.secrets.users.user1.username | quote }}
  password: {{ .Values.secrets.users.user1.password | quote }}

  roles:
    {{- toYaml .Values.secrets.users.user1.roles | nindent 4 }}
