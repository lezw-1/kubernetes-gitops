# Gateway overrides for dev, layered onto apps/gateway/values.yaml
# via the $values ref in argocd/networking/gateway/dev.yaml

global:
  environment: dev

gateway:
  tls:
    enabled: false
    selfSigned: false
  hosts:
    domain: "test.de"
    email: "test@test.de"
  nodePort:
    enabled: true
    http: 32080
    https: 32443
  timeout:
    requestTimeout: 1800s
