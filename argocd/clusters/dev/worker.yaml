# Worker service — LangGraph multi-agent Celery worker, consumes jobs
# dispatched by the api app via the broker (Redis) queue.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  # Application resource name, shown in the Argo CD UI.
  name: worker

  # Namespace the Application resource itself lives in.
  namespace: argocd

spec:
  # Bound to AppProject "default" and its guardrails.
  project: default

  # Single source, rendered by the sops-helm Config Management Plugin
  # (argocd/clusters/dev/values/argocd-cmp.yaml: configs.cmp.plugins.sops-helm)
  # instead of native Helm, since its values file is SOPS-encrypted and
  # native Helm value files can't decrypt it. Checked out at repo root so
  # the plugin can read both the chart (helm/apps/worker) and the values
  # files (argocd/clusters/dev/values/) from one source.
  sources:
    - repoURL: https://github.com/lezw-1/kubernetes-gitops

      # Branch tracked for this source.
      targetRevision: dev

      # Repo root — see comment above for why.
      path: .

      plugin:
        # Must match a key under configs.cmp.plugins in argocd-cmp.yaml.
        name: sops-helm

        env:
          # Path to the worker Helm chart, relative to repo root.
          - name: CHART_PATH
            value: helm/apps/worker

          # Local-cluster overrides, relative to repo root (SOPS-encrypted —
          # only the anthropicApiKey leaf field; the rest stays plaintext —
          # decrypted by the plugin before rendering).
          - name: SECRETS_FILE
            value: argocd/clusters/dev/values/worker.enc.yaml

  destination:
    # Target cluster API server — in-cluster.
    server: https://kubernetes.default.svc

    # Namespace the worker app is deployed into.
    namespace: ai-system-dev

  # Fully automated GitOps sync — no manual apply needed.
  syncPolicy:
    automated:
      # Remove resources no longer defined in Git.
      prune: true

      # Revert manual drift back to match Git.
      selfHeal: true

    syncOptions:
      # Auto-create the destination namespace.
      - CreateNamespace=true

    managedNamespaceMetadata:
      labels:
        # Required for HTTPRoutes to attach to the shared gateway.
        team: gateway-1
